import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    container: {
        flex: 1,
        paddingBottom: 30,
        marginHorizontal: 25,
    },
    bigTitle: {
        fontSize: 30,
        color: 'black',
        fontWeight: 'bold'
    }
});